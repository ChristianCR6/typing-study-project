# Dependencies for analyse.py
# Install with:  python -m pip install -r requirements.txt
#
# Versions pinned to recent stable releases as of 2026 - any reasonably
# modern version of these packages will work.

pandas>=2.0
numpy>=1.24
scipy>=1.10
matplotlib>=3.7
tabulate>=0.9     # required by pandas .to_markdown()
